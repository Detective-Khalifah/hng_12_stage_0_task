# flutter_app

Love is Zero.

## How to Run
1. Install the dependencies:
   Ensuring the [Flutter SDK]() is installed, run
   ```bash
   flutter pub get
   ```

3. Run the app:
   ```bash
   flutter run
   ```